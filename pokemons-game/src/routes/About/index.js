import { useContext } from 'react';
import { PokemonContext } from '../../context/pokemonContext';

const About = () => {

    const padStyle={
        paddingTop: '70px',
    }

    return (
        <div style={padStyle}>
            <h3>About Page</h3>
            
            
        </div>
    )
}

export default About;
